# NOESIS backend environment — copy to `backend/.env`
# All keys are optional; sane defaults are baked into app/config.py.

# --- AI provider -----------------------------------------------------------
# Leave ANTHROPIC_API_KEY blank to run in MOCK mode (no network calls, canned
# diagnostics — useful for UI work and offline demos).
ANTHROPIC_API_KEY=
NOESIS_AI_MODEL=claude-opus-5
NOESIS_AI_THINKING=adaptive
NOESIS_AI_MAX_TOKENS=4096

# --- Server --------------------------------------------------------------
NOESIS_ENV=development
NOESIS_HOST=127.0.0.1
NOESIS_PORT=8000
NOESIS_CORS_ORIGINS=http://localhost:3000

# --- Sandboxed code runner --------------------------------------------------
NOESIS_EXEC_TIMEOUT_SECONDS=5
NOESIS_EXEC_MAX_STEPS=2000
